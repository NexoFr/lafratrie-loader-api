services:
  - type: web
    name: lafratrie-loader-api
    env: php
    buildCommand: "echo 'No build needed'"
    startCommand: "php -S 0.0.0.0:$PORT -t ."
    envVars:
      - key: PHP_VERSION
        value: "8.1"
